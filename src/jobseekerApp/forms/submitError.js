/*import React, { Component } from 'react'
import errorForModal from '../../actions'
import { connect } from 'react-redux'


class SubmitionError extends Component{
	componentWillMount(){
		this.errorForModal()
	}
	render(){
		return console.log(this.props.messageForModal)
	}
}


function mapStateToProps(state) {
  return {
    messageForModal: state.jobseeker.messageForModal
  };
}

export default connect(mapStateToProps, { errorForModal })(SubmitionError)*/