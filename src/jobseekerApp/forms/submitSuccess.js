/*import React, { Component } from 'react'
import successForModal from '../../actions'
import { connect } from 'react-redux'


class SubmitionSuccess extends Component{
	componentWillMount(){
		this.successForModal()
	}
	render(){
		return console.log(this.props.messageForModal)
	}
}


function mapStateToProps(state) {
  return {
    messageForModal: state.jobseeker.messageForModal
  };
}

export default connect(mapStateToProps, { successForModal })(SubmitionSuccess)*/